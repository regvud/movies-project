import React from 'react';
import MovieList from "../MovieList/MovieList";

const MoviesPage = () => {
    return (
        <div>
            {/*HEADER*/}
            <MovieList/>
        </div>
    );
};

export default MoviesPage;